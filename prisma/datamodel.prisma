type Burger {
    id: ID! @id
    createdAt: DateTime! @createdAt
    description: String!
    name: String,
    postedBy: User
}

type User {
    id: ID! @id,
    name: String!,
    email: String! @unique,
    password: String!,
    burgers: [Burger!]!,
}