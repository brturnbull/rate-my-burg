type Query {
    info: String!
    feed: [Burger!]!
    numberOfLinks: String!
}

type Mutation {
    post(url: String!, description: String!): Burger
}

type Burger {
    id: ID!
    description: String!
    name: String!,
    rating: Int! 
}